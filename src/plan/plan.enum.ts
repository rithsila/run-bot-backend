export enum PlanCategory {
    Tools = 'Tools',
    Course = 'Course',
    VPS = 'VPS'
}

export enum CouponStatus {
    Active = 'Active',
    Expired = 'Expired'
}

export enum PlanProducts {
    Indicator = 'Indicator',
    ToolsBox = 'ToolsBox',
    Course = 'Course',
    VPS = 'VPS'
}